<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.hualuomoli.sample.framework.biz.user.mapper.UserMapper">

  <sql id="columns">
    `id`			as "id",
    `username`		as "username",
    `nickname`		as "nickname",
    `age`			as "age",
    `sex`			as "sex",
    `state`  as "state",
    `status`  as "status",
    `remark`		as "remark"
  </sql>
  
  <sql id="querys">
    <if test="id != null"> 
      and `id` = #{id}
    </if>
    <if test="username != null and username != ''"> 
      and `username` = #{username}
    </if>
    <if test="nickname != null and nickname != ''"> 
      and `nickname` = #{nickname}
    </if>
    <if test="age != null"> 
      and `age` = #{age}
    </if>
    <if test="sex != null"> 
      and `sex` = #{sex}
    </if>
    <if test="state != null"> 
      and `state` = #{state}
    </if> 
    <if test="status != null"> 
      and `status` = #{status}
    </if>
    <if test="remark != null and remark != ''"> 
      and `remark` = #{remark}
    </if>
  </sql>
  
  <select id="get" resultType="com.github.hualuomoli.sample.framework.biz.user.entity.User">
    select 
      <include refid="columns" />
    from `t_user`
    where id =  #{id}
  </select>
  
  <insert id="insert">
    insert into `t_user` (
		`id`			,
		`username`		,
		`nickname`		,
		`age`			,
		`sex`			,
    `state`  ,
    `status`  ,
		`remark`		
    ) values (
		#{id},
		#{username},
		#{nickname},
		#{age},
		#{sex},
    #{state},
    #{status},
		#{remark}
    )
  </insert>
  
  <insert id="batchInsert">
    insert into `t_user` (
		`id`			,
		`username`		,
		`nickname`		,
		`age`			,
		`sex`			,
    `state`  ,
    `status`  ,
		`remark`		
    ) 
	<foreach collection="list" item="obj" separator="union all">
	select
		#{obj.id},
		#{obj.username},
		#{obj.nickname},
		#{obj.age},
		#{obj.sex},
    #{obj.state},
    #{obj.status},
		#{obj.remark}
	</foreach>
  </insert>
  
  <update id="update">
    update `t_user`
    <set>
	    <if test="username != null and username != ''"> 
	      `username` = #{username},
	    </if>
	    <if test="nickname != null and nickname != ''"> 
	      `nickname` = #{nickname},
	    </if>
	    <if test="age != null"> 
	      `age` = #{age},
	    </if>
	    <if test="sex != null"> 
	      `sex` = #{sex},
	    </if>
	    <if test="state != null"> 
        `state` = #{state},
      </if>
      <if test="status != null"> 
        `status` = #{status},
      </if>
	    <if test="remark != null and remark != ''"> 
	      `remark` = #{remark},
	    </if>
    </set>
    where id =  #{id}
  </update>
  
  <delete id="delete">
    delete from `t_user` where id =  #{id}
  </delete>
  
  <delete id="batchDelete">
  	delete from `t_user`
  	<where>
  		<foreach collection="ids" item="id" separator="or">
  			id = #{id}
  		</foreach>
  	</where>
  </delete>
  
  <select id="findList" resultType="com.github.hualuomoli.sample.framework.biz.user.entity.User">
    select
      <include refid="columns" />
    from `t_user`
    <where>
      <include refid="querys" />
    </where>
  </select>
  
</mapper>